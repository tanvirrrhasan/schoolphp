# XAMPP সেটআপ গাইড - স্কুল ম্যানেজমেন্ট সিস্টেম

এই গাইডে আপনি XAMPP ব্যবহার করে এই প্রজেক্টটি কীভাবে সেটআপ করবেন তার সম্পূর্ণ ধাপগুলো দেখানো হয়েছে।

## প্রয়োজনীয় সফটওয়্যার

1. **XAMPP** - [ডাউনলোড করুন](https://www.apachefriends.org/download.html)
2. **Node.js** (v18 বা তার উপরে) - [ডাউনলোড করুন](https://nodejs.org/)
3. **Code Editor** (VS Code, PHPStorm ইত্যাদি)

---

## ধাপ ১: XAMPP ইনস্টলেশন

### ১.১ XAMPP ডাউনলোড ও ইনস্টল

1. XAMPP এর অফিসিয়াল ওয়েবসাইট থেকে Windows ভার্শন ডাউনলোড করুন
2. ডাউনলোড করা ফাইলটি রান করুন
3. ইনস্টলেশন প্রক্রিয়ায় "Apache" এবং "MySQL" নির্বাচন করুন
4. ইনস্টলেশন সম্পন্ন হলে XAMPP Control Panel খুলুন

### ১.২ XAMPP স্টার্ট করা

1. XAMPP Control Panel খুলুন
2. **Apache** এর পাশে "Start" বাটন ক্লিক করুন
3. **MySQL** এর পাশে "Start" বাটন ক্লিক করুন
4. উভয় সার্ভিস "Running" (সবুজ) দেখালে সঠিকভাবে কাজ করছে

---

## ধাপ ২: প্রজেক্ট ফোল্ডার সেটআপ

### ২.১ প্রজেক্ট ফোল্ডার কপি করা

1. আপনার প্রজেক্টের সব ফাইল কপি করুন
2. XAMPP এর `htdocs` ফোল্ডারে পেস্ট করুন
   - সাধারণত: `C:\xampp\htdocs\`
3. ফোল্ডারের নাম দিন: `school-management`
   - সম্পূর্ণ পাথ হবে: `C:\xampp\htdocs\school-management\`

### ২.২ ফোল্ডার স্ট্রাকচার

আপনার প্রজেক্ট ফোল্ডারটি এমন দেখাবে:

```
school-management/
├── api/              (PHP API files)
├── app/              (Next.js app files)
├── components/       (React components)
├── database/         (SQL files)
├── lib/              (Library files)
├── public/           (Static files)
├── uploads/          (Uploaded files - auto created)
└── out/              (Build output - after npm run build)
```

---

## ধাপ ৩: ডাটাবেস সেটআপ

### ৩.১ phpMyAdmin খোলা

1. ব্রাউজারে যান: `http://localhost/phpmyadmin`
2. Username: `root` (ডিফল্ট)
3. Password: (খালি রাখুন - XAMPP ডিফল্টে পাসওয়ার্ড নেই)

### ৩.২ ডাটাবেস তৈরি করা

**পদ্ধতি ১: phpMyAdmin দিয়ে**

1. phpMyAdmin এ "New" বা "নতুন" ক্লিক করুন
2. Database name দিন: `school_management`
3. Collation নির্বাচন করুন: `utf8mb4_unicode_ci`
4. "Create" বাটন ক্লিক করুন

**পদ্ধতি ২: SQL ফাইল ইম্পোর্ট**

1. phpMyAdmin এ `school_management` ডাটাবেস নির্বাচন করুন
2. "Import" ট্যাবে যান
3. "Choose File" ক্লিক করুন
4. `database/schema.sql` ফাইল নির্বাচন করুন
5. "Go" বা "যান" বাটন ক্লিক করুন
6. সফল হলে "Import has been successfully finished" মেসেজ দেখাবে

### ৩.৩ ডেমো ডেটা যোগ করা

1. phpMyAdmin এ `school_management` ডাটাবেস নির্বাচন করুন
2. "Import" ট্যাবে যান
3. `database/demo_data.sql` ফাইল নির্বাচন করুন
4. "Go" বাটন ক্লিক করুন
5. ডেমো ডেটা সফলভাবে যোগ হবে

---

## ধাপ ৪: API কনফিগারেশন

### ৪.১ API কনফিগ ফাইল এডিট

1. `api/config.php` ফাইল খুলুন
2. নিম্নলিখিত লাইনগুলো চেক করুন:

```php
define('DB_HOST', 'localhost');
define('DB_USER', 'root');
define('DB_PASS', '');  // XAMPP ডিফল্টে পাসওয়ার্ড নেই
define('DB_NAME', 'school_management');
```

3. যদি আপনার MySQL এ পাসওয়ার্ড থাকে, তাহলে `DB_PASS` এ পাসওয়ার্ড দিন

### ৪.২ Base URL সেট করা

`api/config.php` ফাইলে:

```php
define('BASE_URL', 'http://localhost/school-management/api/uploads/');
```

**মনে রাখবেন:** আপনার ফোল্ডার নাম যদি ভিন্ন হয়, তাহলে BASE_URL এ সেই নাম ব্যবহার করুন।

---

## ধাপ ৫: Node.js Dependencies ইনস্টল

### ৫.১ Terminal/Command Prompt খোলা

1. VS Code এ প্রজেক্ট ফোল্ডার খুলুন
2. Terminal খুলুন (Ctrl + `)
3. অথবা Command Prompt/PowerShell এ প্রজেক্ট ফোল্ডারে যান

### ৫.২ Dependencies ইনস্টল

```bash
npm install
```

এই কমান্ডটি সব প্রয়োজনীয় packages ইনস্টল করবে।

---

## ধাপ ৬: Environment Variables সেট করা

### ৬.১ .env.local ফাইল তৈরি

প্রজেক্টের root ফোল্ডারে `.env.local` নামে একটি ফাইল তৈরি করুন:

```env
NEXT_PUBLIC_API_URL=http://localhost/school-management/api
```

**মনে রাখবেন:** আপনার ফোল্ডার নাম অনুযায়ী URL পরিবর্তন করুন।

---

## ধাপ ৭: প্রজেক্ট বিল্ড করা

### ৭.১ Development Mode (টেস্টিং এর জন্য)

```bash
npm run dev
```

এটি development server চালু করবে। ব্রাউজারে যান: `http://localhost:3000`

### ৭.২ Production Build (হোস্টিং এর জন্য)

```bash
npm run build
```

এই কমান্ডটি:
- সব files compile করবে
- `out/` ফোল্ডারে static files তৈরি করবে
- এই `out/` ফোল্ডারটি hosting এ upload করতে হবে

**বিল্ড সম্পন্ন হলে:**
- `out/` ফোল্ডারটি `htdocs/school-management/` এ কপি করুন
- অথবা `out/` ফোল্ডারের content সরাসরি `htdocs/school-management/` এ move করুন

---

## ধাপ ৮: Apache Configuration (ঐচ্ছিক)

### ৮.১ .htaccess ফাইল

`out/` ফোল্ডারে (বা root ফোল্ডারে) `.htaccess` ফাইল তৈরি করুন:

```apache
RewriteEngine On
RewriteBase /school-management/

# Handle Next.js routing
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^(.*)$ /school-management/index.html [L]

# API requests
RewriteCond %{REQUEST_URI} ^/school-management/api/
RewriteRule ^api/(.*)$ api/$1 [L]
```

### ৮.২ mod_rewrite Enable করা

1. `C:\xampp\apache\conf\httpd.conf` ফাইল খুলুন
2. এই লাইনটি খুঁজুন: `#LoadModule rewrite_module modules/mod_rewrite.so`
3. `#` চিহ্নটি সরিয়ে দিন
4. Apache restart করুন

---

## ধাপ ৯: ফাইল পারমিশন (Linux/Mac)

যদি Linux বা Mac ব্যবহার করেন:

```bash
chmod -R 755 uploads/
chmod -R 755 api/
```

Windows এ সাধারণত এই সমস্যা হয় না।

---

## ধাপ ১০: টেস্ট করা

### ১০.১ API টেস্ট

ব্রাউজারে যান: `http://localhost/school-management/api/crud.php?table=teachers`

এটি teachers এর list দেখাবে (JSON format এ)।

### ১০.২ Frontend টেস্ট

1. `npm run dev` চালু করুন
2. ব্রাউজারে যান: `http://localhost:3000`
3. Homepage দেখতে পাবেন

### ১০.৩ Admin Panel টেস্ট

1. `http://localhost:3000/admin/login` এ যান
2. Login করুন:
   - Email: `admin@school.com`
   - Password: `admin123`

---

## সমস্যা সমাধান (Troubleshooting)

### সমস্যা ১: "Database connection failed"

**সমাধান:**
- MySQL XAMPP Control Panel এ running আছে কিনা চেক করুন
- `api/config.php` এ database credentials সঠিক কিনা দেখুন
- phpMyAdmin এ database তৈরি হয়েছে কিনা নিশ্চিত করুন

### সমস্যা ২: "404 Not Found" API requests

**সমাধান:**
- `.env.local` ফাইলে `NEXT_PUBLIC_API_URL` সঠিক কিনা চেক করুন
- API files `api/` ফোল্ডারে আছে কিনা দেখুন
- Apache running আছে কিনা নিশ্চিত করুন

### সমস্যা ৩: Images load হচ্ছে না

**সমাধান:**
- `uploads/` ফোল্ডার তৈরি হয়েছে কিনা দেখুন
- `api/config.php` এ `BASE_URL` সঠিক কিনা চেক করুন
- File permissions চেক করুন

### সমস্যা ৪: "CORS error"

**সমাধান:**
- `api/config.php` ফাইলে CORS headers আছে কিনা দেখুন
- Browser cache clear করুন

### সমস্যা ৫: Build error

**সমাধান:**
- `node_modules` ফোল্ডার delete করুন
- `npm install` আবার run করুন
- `npm run build` আবার চেষ্টা করুন

---

## Production Hosting

### Real Hosting এ Deploy করা

1. **Database Setup:**
   - Hosting provider এর cPanel এ phpMyAdmin খুলুন
   - `schema.sql` import করুন
   - `demo_data.sql` import করুন (ঐচ্ছিক)

2. **Files Upload:**
   - `out/` ফোল্ডারের সব files FTP/cPanel File Manager দিয়ে upload করুন
   - `api/` ফোল্ডার upload করুন
   - `uploads/` ফোল্ডার তৈরি করুন (permissions: 755)

3. **Configuration:**
   - `api/config.php` এ database credentials আপডেট করুন
   - `BASE_URL` আপনার domain অনুযায়ী পরিবর্তন করুন
   - `.env.local` এ `NEXT_PUBLIC_API_URL` আপডেট করুন

4. **Build Again:**
   - Local এ `npm run build` run করুন
   - `out/` ফোল্ডার upload করুন

---

## গুরুত্বপূর্ণ নোট

1. **Security:**
   - Production এ MySQL password অবশ্যই set করুন
   - `.env.local` file কখনো git এ commit করবেন না
   - File upload size limit check করুন

2. **Backup:**
   - নিয়মিত database backup নিন
   - `uploads/` ফোল্ডার backup করুন

3. **Updates:**
   - Database schema পরিবর্তন হলে migration script ব্যবহার করুন
   - API changes এর সময় frontend এও update করুন

---

## সহায়তা

যদি কোনো সমস্যা হয়:

1. Error messages carefully পড়ুন
2. Browser console (F12) check করুন
3. Apache error logs check করুন: `C:\xampp\apache\logs\error.log`
4. MySQL error logs check করুন: `C:\xampp\mysql\data\*.err`

---

## সম্পন্ন!

এখন আপনার স্কুল ম্যানেজমেন্ট সিস্টেম XAMPP এ সেটআপ সম্পন্ন হয়েছে। 

**Admin Login:**
- URL: `http://localhost:3000/admin/login`
- Email: `admin@school.com`
- Password: `admin123`

শুভকামনা! 🎉

